#!/usr/bin/env python
from argparse import ArgumentParser
from os import CLONE_NEWUSER, chdir, chroot, execl, unshare

ap = ArgumentParser()
ap.add_argument('newroot')
args = ap.parse_args()

unshare(CLONE_NEWUSER)  # Gain us CAP_SYS_CHROOT
chdir(args.newroot)
chroot('.')
execl('bin/sh', 'sh')
